import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const medicalHistoryImporterSelectors = importerSelectors(
  'medicalHistory.importer',
);

export default medicalHistoryImporterSelectors;
