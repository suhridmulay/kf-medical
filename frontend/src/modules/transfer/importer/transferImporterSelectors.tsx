import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const transferImporterSelectors = importerSelectors(
  'transfer.importer',
);

export default transferImporterSelectors;
