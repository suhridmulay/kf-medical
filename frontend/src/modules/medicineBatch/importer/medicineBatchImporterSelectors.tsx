import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const medicineBatchImporterSelectors = importerSelectors(
  'medicineBatch.importer',
);

export default medicineBatchImporterSelectors;
