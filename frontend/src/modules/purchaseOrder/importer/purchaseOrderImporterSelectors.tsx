import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const purchaseOrderImporterSelectors = importerSelectors(
  'purchaseOrder.importer',
);

export default purchaseOrderImporterSelectors;
